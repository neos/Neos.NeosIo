-
  requestFilter: 'parentRequest.isPackage("Neos.Neos") && isFormat("html") && (isController("Paginate") && isAction("index"))'
  options:
    templatePathAndFilename: 'resource://Neos.MarketPlace/Private/Templates/ViewHelpers/Widget/Paginate/Index.html'
