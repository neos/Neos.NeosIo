prototype(Neos.MarketPlace:PackageSourceButton) < prototype(Neos.MarketPlace:Button) {
    content = ${this.url ? 'Go to the source' : null}
    url = ${q(node).property('repository')}
    openInNewTab = true
}
