prototype(Sitegeist.Monocle:Preview.Page) {
    prototype(Neos.NeosIo:PageStyles) {
        // TODO: Remove this once, when all is done
        @if.false = false
    }
    head.assets = Neos.Presentation:Atom.StyleJavascript
}
