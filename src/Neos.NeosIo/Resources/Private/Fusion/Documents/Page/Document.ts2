prototype(Neos.NodeTypes:Page.Document) < prototype(Neos.NeosIo:DefaultPage) {
    body {
        content {
            // The default content section
            main = PrimaryContent {
                nodePath = 'main'
            }
        }
    }
}
